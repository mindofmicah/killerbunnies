define(['modules/history/main'], function (History) {
    describe('History Module', function () {
        it('should exist', function () {
            expect(History).toBeDefined();
        });
        describe('History.Model', function () {
            it('should be defined', function () {
                expect(History.Model).toBeDefined();
            });
            it('should return a new model', function () {
                
            });
        });
    });
});
